using UnityEngine;
using System.Collections;

public class DeployTestManager : MonoBehaviour {

	/*
	 * Test manager
	 * When the game is built to web there are certain behaviors and features we will want to test
	 * 
	 * Handling menus such as pause, level editor
	 * Clicking through the Tutorials
	 * Placing one of each object onto the map
	 * Editing all editable features of each object we place
	 * Completing a save
	 * Completing a map change
	 * 
	 * 
	 * */
	
	// Update is called once per frame
	void Update () {
//		if (Input.GetKeyDown(KeyCode(T)) && Input.GetKeyDown(KeyCode.E) && Input.GetKeyDown(KeyCode.S)){
//			BeginTest();
//		}
	}
}
