using UnityEngine;
using System.Collections;

public class FollowCameraRotationY : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		Quaternion rot = new Quaternion();
//		rot.eulerAngles = new Vector3 (transform.eulerAngles.x,
	}
}
